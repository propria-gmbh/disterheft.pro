# Known Differences

1) **Mobile layout parity**
   - The source site serves a separate mobile layout via Wix.
   - This implementation uses responsive CSS to mirror that layout.
   - Minor spacing/position differences may remain on small screens.

2) **Consent banner implementation details**
   - No prior consent banner code/spec was provided.
   - Storage key and gating rules were implemented as documented in `audit/consent-behavior.md`.
